always @(*) begin
	if((active1=1'b0)&(active2=1'b0)) begin
		_io_out_buf_  : assert(io_out       == buf_io_out);
	end

	if((active1!=0)|(active2!=0)) begin
		_io_out_z_    : assert(io_out       == `MPRJ_IO_PADS'b0);
	end
end

